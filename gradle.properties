# Enable Gradle build cache and set reasonable JVM args for CI/local builds
org.gradle.caching=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.workers.max=4

# Keep wrapper verification (if used)
org.gradle.wrapper.validation=true